#include<iostream>
int main()
{
	int a,b,c,d;
	a=10;
	b=100;
	c=1000;
	d=12;
	if(a>b && a>c && a>d)
	{
		std::cout<<"a is greater";
	}
	else if(b>a && b>c && b>d)
	{
		std::cout<<"b is greater";
	}
	else if(c>a && c>b && c>d)
	{
		std::cout<<"c is greater";
	}
	else
	{
		std::cout<<"d is greater";
	}
	return 0;
}
